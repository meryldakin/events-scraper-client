import React, { Component } from "react";
import { getEvents } from "../api";
import { Switch, Route } from "react-router-dom";

function Events(props) {
  return <div> Events go here! </div>;
}

export default Events;
